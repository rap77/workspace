export function utils(): string {
  return 'utils';
}

/**
 * Metogo para saludar al usuario
 * @param name nombre del usuario
 */
export function greet(name: string): string {
  return `Hola ${name}`;
}

export class Person {}
